# Dependencies for the figma-implement skill scripts.
# Install with: pip install -r scripts/requirements.txt --break-system-packages
#
# If using a venv instead, drop --break-system-packages:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r scripts/requirements.txt

# Phase 4 — figma-gate-visual.py (pixel diff + live screenshot)
numpy>=1.26
Pillow>=10.0
playwright>=1.40
# After pip install, the Chromium browser binary must also be installed once:
#   playwright install chromium

# Phase 5 — figma-gate-quality.py (a11y check via axe-core engine)
axe-playwright-python>=0.1.7
# Also requires Node.js + pnpm on PATH for the tsc / lint checks (not pip-installable).
