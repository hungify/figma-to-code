node_modules

# Ignore lockfiles we don't use
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
# bun.lock

.DS_Store
.cache
.figma/cache/
.env

.data
.vercel
.output
.wrangler
.netlify
dist
/build/
/api/
/server/build
/public/build

.tanstack

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Figma fidelity run outputs — entire tree ephemeral (gold/meta/actual/diff/score/…).
# Re-fetch + re-run via fidelity_*; do not commit. Keep `.figma/prop-map/` (committed).
.figma/artifacts/
eval/figma-screens/last-scorecard.json
