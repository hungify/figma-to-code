import { test } from "../support/fixtures";

/**
 * Placeholder so `pnpm test:visual` passes with an empty suite.
 * Replace with real specs under e2e/visual/<feature>/<screen>.visual.spec.ts
 * when a Figma screen is implemented (see e2e/README.md).
 */
test.describe("figma screens @visual", () => {
  test.skip(true, "No Figma implement screens yet — add *.visual.spec.ts per screen");

  test("placeholder", () => {
    // intentionally empty
  });
});
